import React from "react";

const AppError = () => (
  <div className="app-error">
    Sorry, we have detected an error in processing! Please refresh the application to continue.
  </div>
);
export default AppError;
